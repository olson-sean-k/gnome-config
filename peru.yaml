imports:
  ext-multiple-monitor-panels: extensions/

git module ext-multiple-monitor-panels:
  url: https://github.com/jbladen/multiple-monitor-panels.git
  rev: gnome-3.10
  build: mkdir out && cp -R extra-panels@darkxst.feathertop.org out/
  export: out

build: ./install.sh
